apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: dwk-cluster-gateway
  namespace: default
spec:
  gatewayClassName: eg
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      kinds:
      - kind: HTTPRoute
      namespaces:
        from: All
