apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      volumes:
        - name: todo-app
          persistentVolumeClaim:
            claimName: todo-app
      containers:
        - name: todo-app
          image: fallmx/todo-app:1.13
          volumeMounts:
          - name: todo-app
            mountPath: /mnt/todo-app
          env:
          - name: PORT
            value: "8081"
          - name: PICTURE
            value: "/mnt/todo-app/picture.jpg"
          - name: PICTURE_API
            value: "https://picsum.photos/600"
          - name: GIN_MODE
            value: release

