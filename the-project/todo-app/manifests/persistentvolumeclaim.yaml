apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: project
  name: todo-app
spec:
  storageClassName: todo-app
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
